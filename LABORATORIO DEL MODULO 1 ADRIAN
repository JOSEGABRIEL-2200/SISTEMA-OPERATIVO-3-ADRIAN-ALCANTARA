Practica 2: Configuración de parametros de red.
─$ history
    1  ip a
    2  sudo ip addr add 192.168.1.50/24 dev eth0
    3  sudo ip route add default via 192.168.1.1
    4  ip a
    5  sudo nano /etc/resolv.conf
    6  ip a

Practica 3: Gestion de Usuario y grupos
 sudo adduser jose 
    8  sudo adduser JOSE 
    9  sudo usermod -aG sudo jose 
   10  sudo usermod -aG sudo JOSE 
   11  id JOSE 
   12  sudo groupadd guest 
   13  sudo groupadd guestt 
   14  sudo adduser -aG gusestt invitado  
   15  sudo adduser invitado 
   16  sudo adduser -aG gusestt invitado  
   17  sudo usermod -aG gusestt invitado  
   18  sudo usermod -aG gusett invitado  
   19  sudo usermod -aG guestt invitado 
   20  geten group guestt
   21  getent group guestt
   22  sudo deluser invitado 
   23  sudo groupdel  guestt 
   24  cat /etc/passwd | grep invitado 
   25  getent group guestt

Práctica 4: Gestión de Permisos de Archivos
mkdir materia 
   33  cd materia 
   34  nano estudiante.txt
   35  ls -l
   36  chmod 700 estudiante.txt 
   37  ls -l estudiante.txt
   38  chmod 070  estudiante.txt 
   39  ls -l estudiante.txt
   40  mkdir ../materia 2
   41  mkdir ../materia2
   42  cp estudiante.txt ../materia2/
   43  ls -l ../materia2
   44  ls -l ../materia
   45  ls -l ../materia2
   46  cd .. 
   47  rm -r materia 
   48  ls
   49  rm -r materia24
   50  rm -r materia2
   51  ls


